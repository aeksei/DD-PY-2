type: theory
files:
- name: main.py
  visible: true
  placeholders:
  - offset: 99
    length: 310
    placeholder_text: |-
      # TODO сделать методом класса
          def area(self, *args):
              """
              Метод, который считает площадь по разным формулам,
               в зависимости от количества переданных аргументов.
              """
              if len(args) == 2:
                  self.area_by_height(*args)
              if len(args) == 3:
                  self.area_by_angle(*args)
  - offset: 415
    length: 49
    placeholder_text: 'def area_by_angle(self, a, b, angle):  # TODO сделать статическим
      методом'
  - offset: 583
    length: 43
    placeholder_text: 'def area_by_height(self, a, h):  # TODO сделать статическим
      методом'
- name: __init__.py
  visible: false
